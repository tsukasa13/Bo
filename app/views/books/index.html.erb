<h2>User info</h2>

<%= form_with model: @book do |f| %>

  <% if @book.image.attached? %>
    <%= image_tag book.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200x200" %>
  <% end %>

  <table>
    <tbody>
      <tr>
        <th>name</th>
        <th><%= current_user.name %></th>
      </tr>
      <tr>
        <th>introduction</th>
        <th></th>
        <th><%= link_to "プロフィール編集", "users/edit" %></th>
      </tr>
    </tbody>
  </table>

<% end %>

<h2>New book</h2>

<%= form_with model: @book do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Opinion</h4>
  <%= f.text_area :body %>

  <br><%= f.submit 'Create Book' %></br>
<% end %>
